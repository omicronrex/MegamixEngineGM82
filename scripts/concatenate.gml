/// concatenate(arrays...)
// returns a new array which contains the contents of all the provided
// arrays in order
// if non-array arguments are provided, they are inserted into the array
// array in order as well.

show_error("concatenate",0)
